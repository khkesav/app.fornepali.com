# Railway deployment configuration for Flask
# This file tells Railway to use Python 3.11 and install dependencies from requirements.txt
[build]
  pythonVersion = "3.11"
  command = "pip install -r requirements.txt"

[start]
  command = "python app.py"
